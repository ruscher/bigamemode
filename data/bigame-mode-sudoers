# Allow members of the wheel group to run bigame-daemon without a password
# or allow bigame-ui to run specific commands via sudo if needed.
# However, we are moving to D-Bus. This file is kept for compatibility.

%wheel ALL=(ALL) NOPASSWD: /usr/bin/bigame-daemon
%wheel ALL=(ALL) NOPASSWD: /usr/bin/falcond
