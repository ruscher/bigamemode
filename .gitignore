# Rust
target/

# makepkg working directories and packages
/src/
/pkg/
/pkgbuild/
*.pkg.tar
*.pkg.tar.zst
*.pkg.tar.xz

# Compiled translations (built by the PKGBUILD)
*.mo

# Editors and local tools
*.swp
*~
*.bak
*.orig
.idea/
.vscode/
*.iml
.serena/
.DS_Store
__pycache__/

# SuperTuxKart's per-frame, per-stage render profile: about 4 MB per run and
# 98% of a session's weight. Summaries, frame counts and GPU telemetry are
# what the published results rest on; this is left beside them, uncommitted.
benchmarks/**/stdout.log.profile-*.csv
